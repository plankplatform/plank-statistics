# Plank Statistics

### Release su dev

npm run build

### Release in prod

npm run release

commit and push

git tag vx.x.x

git push origin vx.x.x

modificare la versione in snippet PHP

commit e push in PageEvents
